apiVersion: v1
kind: Service
metadata:
  name: {{ include "mylar.fullname" . }}
  labels:
    {{- include "mylar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mylar.selectorLabels" . | nindent 4 }}
