apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "traefik_home.fullname" . }}-test-connection"
  labels:
    {{- include "traefik_home.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "traefik_home.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
