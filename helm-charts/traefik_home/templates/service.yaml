apiVersion: v1
kind: Service
metadata:
  name: {{ include "traefik_home.fullname" . }}
  labels:
    {{- include "traefik_home.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "traefik_home.selectorLabels" . | nindent 4 }}
