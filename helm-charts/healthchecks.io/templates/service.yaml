apiVersion: v1
kind: Service
metadata:
  name: {{ include "healthchecks.io.fullname" . }}
  labels:
    {{- include "healthchecks.io.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "healthchecks.io.selectorLabels" . | nindent 4 }}
