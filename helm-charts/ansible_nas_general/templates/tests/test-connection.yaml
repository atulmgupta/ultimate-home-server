apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ansible_nas_general.fullname" . }}-test-connection"
  labels:
    {{- include "ansible_nas_general.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ansible_nas_general.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
