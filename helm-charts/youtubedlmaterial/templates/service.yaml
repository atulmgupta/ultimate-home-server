apiVersion: v1
kind: Service
metadata:
  name: {{ include "youtubedlmaterial.fullname" . }}
  labels:
    {{- include "youtubedlmaterial.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "youtubedlmaterial.selectorLabels" . | nindent 4 }}
