apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "youtubedlmaterial.fullname" . }}-test-connection"
  labels:
    {{- include "youtubedlmaterial.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "youtubedlmaterial.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
