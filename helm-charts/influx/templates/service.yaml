apiVersion: v1
kind: Service
metadata:
  name: {{ include "influx.fullname" . }}
  labels:
    {{- include "influx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "influx.selectorLabels" . | nindent 4 }}
