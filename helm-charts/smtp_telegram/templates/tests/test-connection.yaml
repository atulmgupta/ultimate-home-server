apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "smtp_telegram.fullname" . }}-test-connection"
  labels:
    {{- include "smtp_telegram.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "smtp_telegram.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
