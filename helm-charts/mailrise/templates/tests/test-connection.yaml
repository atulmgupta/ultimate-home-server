apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mailrise.fullname" . }}-test-connection"
  labels:
    {{- include "mailrise.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mailrise.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
