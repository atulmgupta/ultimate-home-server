apiVersion: v1
kind: Service
metadata:
  name: {{ include "hammond.fullname" . }}
  labels:
    {{- include "hammond.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hammond.selectorLabels" . | nindent 4 }}
