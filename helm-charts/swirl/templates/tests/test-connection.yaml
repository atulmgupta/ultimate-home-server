apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "swirl.fullname" . }}-test-connection"
  labels:
    {{- include "swirl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "swirl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
