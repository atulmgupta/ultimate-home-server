apiVersion: v1
kind: Service
metadata:
  name: {{ include "rsync.fullname" . }}
  labels:
    {{- include "rsync.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rsync.selectorLabels" . | nindent 4 }}
