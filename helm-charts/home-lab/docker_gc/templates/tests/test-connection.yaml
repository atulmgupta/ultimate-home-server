apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "docker_gc.fullname" . }}-test-connection"
  labels:
    {{- include "docker_gc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "docker_gc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
