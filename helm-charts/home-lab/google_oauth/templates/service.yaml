apiVersion: v1
kind: Service
metadata:
  name: {{ include "google_oauth.fullname" . }}
  labels:
    {{- include "google_oauth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "google_oauth.selectorLabels" . | nindent 4 }}
