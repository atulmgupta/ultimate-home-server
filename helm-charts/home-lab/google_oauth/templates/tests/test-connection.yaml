apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "google_oauth.fullname" . }}-test-connection"
  labels:
    {{- include "google_oauth.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "google_oauth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
