apiVersion: v1
kind: Service
metadata:
  name: {{ include "dockly.fullname" . }}
  labels:
    {{- include "dockly.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dockly.selectorLabels" . | nindent 4 }}
