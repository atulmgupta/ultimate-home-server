apiVersion: v1
kind: Service
metadata:
  name: {{ include "file_manager.fullname" . }}
  labels:
    {{- include "file_manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "file_manager.selectorLabels" . | nindent 4 }}
