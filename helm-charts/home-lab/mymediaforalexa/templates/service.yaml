apiVersion: v1
kind: Service
metadata:
  name: {{ include "mymediaforalexa.fullname" . }}
  labels:
    {{- include "mymediaforalexa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mymediaforalexa.selectorLabels" . | nindent 4 }}
