apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rsnapshot.fullname" . }}-test-connection"
  labels:
    {{- include "rsnapshot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rsnapshot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
