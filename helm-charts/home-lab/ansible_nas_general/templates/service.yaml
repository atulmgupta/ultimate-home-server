apiVersion: v1
kind: Service
metadata:
  name: {{ include "ansible_nas_general.fullname" . }}
  labels:
    {{- include "ansible_nas_general.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ansible_nas_general.selectorLabels" . | nindent 4 }}
