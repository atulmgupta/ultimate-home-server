apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hchecks.fullname" . }}-test-connection"
  labels:
    {{- include "hchecks.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hchecks.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
