apiVersion: v1
kind: Service
metadata:
  name: {{ include "pytivo.fullname" . }}
  labels:
    {{- include "pytivo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pytivo.selectorLabels" . | nindent 4 }}
