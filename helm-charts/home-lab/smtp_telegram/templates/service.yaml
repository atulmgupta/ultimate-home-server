apiVersion: v1
kind: Service
metadata:
  name: {{ include "smtp_telegram.fullname" . }}
  labels:
    {{- include "smtp_telegram.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smtp_telegram.selectorLabels" . | nindent 4 }}
