apiVersion: v1
kind: Service
metadata:
  name: {{ include "smb_shares.fullname" . }}
  labels:
    {{- include "smb_shares.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smb_shares.selectorLabels" . | nindent 4 }}
