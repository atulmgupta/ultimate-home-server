apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "smb_shares.fullname" . }}-test-connection"
  labels:
    {{- include "smb_shares.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "smb_shares.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
