apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "doublecommander.fullname" . }}-test-connection"
  labels:
    {{- include "doublecommander.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "doublecommander.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
