apiVersion: v1
kind: Service
metadata:
  name: {{ include "doublecommander.fullname" . }}
  labels:
    {{- include "doublecommander.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "doublecommander.selectorLabels" . | nindent 4 }}
