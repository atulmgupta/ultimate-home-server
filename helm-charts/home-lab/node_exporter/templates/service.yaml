apiVersion: v1
kind: Service
metadata:
  name: {{ include "node_exporter.fullname" . }}
  labels:
    {{- include "node_exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "node_exporter.selectorLabels" . | nindent 4 }}
