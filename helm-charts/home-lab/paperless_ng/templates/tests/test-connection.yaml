apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "paperless_ng.fullname" . }}-test-connection"
  labels:
    {{- include "paperless_ng.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "paperless_ng.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
