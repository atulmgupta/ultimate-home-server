apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stats.fullname" . }}-test-connection"
  labels:
    {{- include "stats.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stats.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
