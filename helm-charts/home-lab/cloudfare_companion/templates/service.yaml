apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudfare_companion.fullname" . }}
  labels:
    {{- include "cloudfare_companion.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloudfare_companion.selectorLabels" . | nindent 4 }}
