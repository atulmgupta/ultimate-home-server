apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloudfare_companion.fullname" . }}-test-connection"
  labels:
    {{- include "cloudfare_companion.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloudfare_companion.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
