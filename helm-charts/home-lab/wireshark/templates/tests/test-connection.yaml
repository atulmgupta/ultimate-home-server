apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wireshark.fullname" . }}-test-connection"
  labels:
    {{- include "wireshark.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wireshark.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
