apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sample_nginx_service.fullname" . }}-test-connection"
  labels:
    {{- include "sample_nginx_service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sample_nginx_service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
