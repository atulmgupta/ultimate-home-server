apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample_nginx_service.fullname" . }}
  labels:
    {{- include "sample_nginx_service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample_nginx_service.selectorLabels" . | nindent 4 }}
