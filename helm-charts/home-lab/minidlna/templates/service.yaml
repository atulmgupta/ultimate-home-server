apiVersion: v1
kind: Service
metadata:
  name: {{ include "minidlna.fullname" . }}
  labels:
    {{- include "minidlna.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "minidlna.selectorLabels" . | nindent 4 }}
