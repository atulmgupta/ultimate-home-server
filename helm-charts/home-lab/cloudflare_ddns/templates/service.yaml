apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudflare_ddns.fullname" . }}
  labels:
    {{- include "cloudflare_ddns.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloudflare_ddns.selectorLabels" . | nindent 4 }}
