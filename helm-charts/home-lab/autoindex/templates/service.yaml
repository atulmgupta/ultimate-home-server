apiVersion: v1
kind: Service
metadata:
  name: {{ include "autoindex.fullname" . }}
  labels:
    {{- include "autoindex.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "autoindex.selectorLabels" . | nindent 4 }}
