apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "autoindex.fullname" . }}-test-connection"
  labels:
    {{- include "autoindex.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "autoindex.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
