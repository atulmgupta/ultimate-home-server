apiVersion: v1
kind: Service
metadata:
  name: {{ include "calibre_web.fullname" . }}
  labels:
    {{- include "calibre_web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "calibre_web.selectorLabels" . | nindent 4 }}
