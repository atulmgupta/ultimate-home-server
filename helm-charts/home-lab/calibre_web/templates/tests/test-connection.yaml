apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calibre_web.fullname" . }}-test-connection"
  labels:
    {{- include "calibre_web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "calibre_web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
