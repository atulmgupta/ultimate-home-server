apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "influx.fullname" . }}-test-connection"
  labels:
    {{- include "influx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "influx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
