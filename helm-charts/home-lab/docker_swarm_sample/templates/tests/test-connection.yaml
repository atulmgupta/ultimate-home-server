apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "docker_swarm_sample.fullname" . }}-test-connection"
  labels:
    {{- include "docker_swarm_sample.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "docker_swarm_sample.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
