apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rsync.fullname" . }}-test-connection"
  labels:
    {{- include "rsync.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rsync.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
