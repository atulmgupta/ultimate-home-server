apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kv.fullname" . }}-test-connection"
  labels:
    {{- include "kv.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kv.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
