apiVersion: v1
kind: Service
metadata:
  name: {{ include "kv.fullname" . }}
  labels:
    {{- include "kv.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kv.selectorLabels" . | nindent 4 }}
