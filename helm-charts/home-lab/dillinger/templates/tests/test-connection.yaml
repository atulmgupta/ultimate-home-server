apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dillinger.fullname" . }}-test-connection"
  labels:
    {{- include "dillinger.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dillinger.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
