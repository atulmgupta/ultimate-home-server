apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mosquito.fullname" . }}-test-connection"
  labels:
    {{- include "mosquito.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mosquito.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
