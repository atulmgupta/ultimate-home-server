apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginxerror.fullname" . }}
  labels:
    {{- include "nginxerror.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginxerror.selectorLabels" . | nindent 4 }}
