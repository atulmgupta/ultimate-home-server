apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginxerror.fullname" . }}-test-connection"
  labels:
    {{- include "nginxerror.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginxerror.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
