apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "diun.fullname" . }}-test-connection"
  labels:
    {{- include "diun.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "diun.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
