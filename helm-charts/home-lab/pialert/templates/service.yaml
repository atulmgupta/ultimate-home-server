apiVersion: v1
kind: Service
metadata:
  name: {{ include "pialert.fullname" . }}
  labels:
    {{- include "pialert.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pialert.selectorLabels" . | nindent 4 }}
