apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qdirststat.fullname" . }}-test-connection"
  labels:
    {{- include "qdirststat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qdirststat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
