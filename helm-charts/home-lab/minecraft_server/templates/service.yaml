apiVersion: v1
kind: Service
metadata:
  name: {{ include "minecraft_server.fullname" . }}
  labels:
    {{- include "minecraft_server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "minecraft_server.selectorLabels" . | nindent 4 }}
