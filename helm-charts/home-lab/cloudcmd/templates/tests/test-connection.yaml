apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloudcmd.fullname" . }}-test-connection"
  labels:
    {{- include "cloudcmd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloudcmd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
