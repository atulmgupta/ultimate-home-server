apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gladys.fullname" . }}-test-connection"
  labels:
    {{- include "gladys.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gladys.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
