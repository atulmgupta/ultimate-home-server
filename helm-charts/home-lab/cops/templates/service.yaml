apiVersion: v1
kind: Service
metadata:
  name: {{ include "cops.fullname" . }}
  labels:
    {{- include "cops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cops.selectorLabels" . | nindent 4 }}
