apiVersion: v1
kind: Service
metadata:
  name: {{ include "swirl.fullname" . }}
  labels:
    {{- include "swirl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "swirl.selectorLabels" . | nindent 4 }}
