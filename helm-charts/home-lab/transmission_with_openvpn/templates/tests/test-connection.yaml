apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "transmission_with_openvpn.fullname" . }}-test-connection"
  labels:
    {{- include "transmission_with_openvpn.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "transmission_with_openvpn.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
