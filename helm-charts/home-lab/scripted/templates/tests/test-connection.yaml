apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scripted.fullname" . }}-test-connection"
  labels:
    {{- include "scripted.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scripted.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
