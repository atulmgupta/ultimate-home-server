apiVersion: v1
kind: Service
metadata:
  name: {{ include "scripted.fullname" . }}
  labels:
    {{- include "scripted.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scripted.selectorLabels" . | nindent 4 }}
