apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginxproxymanager.fullname" . }}
  labels:
    {{- include "nginxproxymanager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginxproxymanager.selectorLabels" . | nindent 4 }}
