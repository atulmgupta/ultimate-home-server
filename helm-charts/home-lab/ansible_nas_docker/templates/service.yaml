apiVersion: v1
kind: Service
metadata:
  name: {{ include "ansible_nas_docker.fullname" . }}
  labels:
    {{- include "ansible_nas_docker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ansible_nas_docker.selectorLabels" . | nindent 4 }}
