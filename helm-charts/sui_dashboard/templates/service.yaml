apiVersion: v1
kind: Service
metadata:
  name: {{ include "sui_dashboard.fullname" . }}
  labels:
    {{- include "sui_dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sui_dashboard.selectorLabels" . | nindent 4 }}
