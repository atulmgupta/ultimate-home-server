apiVersion: v1
kind: Service
metadata:
  name: {{ include "syslog.fullname" . }}
  labels:
    {{- include "syslog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "syslog.selectorLabels" . | nindent 4 }}
