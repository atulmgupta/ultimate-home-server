apiVersion: v1
kind: Service
metadata:
  name: {{ include "docker_proxy.fullname" . }}
  labels:
    {{- include "docker_proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "docker_proxy.selectorLabels" . | nindent 4 }}
