apiVersion: v1
kind: Service
metadata:
  name: {{ include "mosquito.fullname" . }}
  labels:
    {{- include "mosquito.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mosquito.selectorLabels" . | nindent 4 }}
