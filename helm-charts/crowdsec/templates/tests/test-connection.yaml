apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crowdsec.fullname" . }}-test-connection"
  labels:
    {{- include "crowdsec.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crowdsec.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
