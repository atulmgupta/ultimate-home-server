apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mysql_common.fullname" . }}-test-connection"
  labels:
    {{- include "mysql_common.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mysql_common.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
