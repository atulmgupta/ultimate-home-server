apiVersion: v1
kind: Service
metadata:
  name: {{ include "paperless_ng.fullname" . }}
  labels:
    {{- include "paperless_ng.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "paperless_ng.selectorLabels" . | nindent 4 }}
