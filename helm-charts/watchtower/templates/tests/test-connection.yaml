apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "watchtower.fullname" . }}-test-connection"
  labels:
    {{- include "watchtower.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "watchtower.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
