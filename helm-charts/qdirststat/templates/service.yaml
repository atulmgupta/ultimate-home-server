apiVersion: v1
kind: Service
metadata:
  name: {{ include "qdirststat.fullname" . }}
  labels:
    {{- include "qdirststat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qdirststat.selectorLabels" . | nindent 4 }}
