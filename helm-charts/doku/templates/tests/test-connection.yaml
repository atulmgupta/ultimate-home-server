apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "doku.fullname" . }}-test-connection"
  labels:
    {{- include "doku.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "doku.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
