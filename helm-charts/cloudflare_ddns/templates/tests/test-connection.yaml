apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloudflare_ddns.fullname" . }}-test-connection"
  labels:
    {{- include "cloudflare_ddns.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloudflare_ddns.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
