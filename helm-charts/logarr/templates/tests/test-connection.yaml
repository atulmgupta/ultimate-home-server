apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "logarr.fullname" . }}-test-connection"
  labels:
    {{- include "logarr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "logarr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
