apiVersion: v1
kind: Service
metadata:
  name: {{ include "google_ddns.fullname" . }}
  labels:
    {{- include "google_ddns.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "google_ddns.selectorLabels" . | nindent 4 }}
