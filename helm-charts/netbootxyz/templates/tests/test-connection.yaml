apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "netbootxyz.fullname" . }}-test-connection"
  labels:
    {{- include "netbootxyz.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "netbootxyz.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
