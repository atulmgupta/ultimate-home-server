apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ansible_nas_docker.fullname" . }}-test-connection"
  labels:
    {{- include "ansible_nas_docker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ansible_nas_docker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
