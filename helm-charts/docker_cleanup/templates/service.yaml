apiVersion: v1
kind: Service
metadata:
  name: {{ include "docker_cleanup.fullname" . }}
  labels:
    {{- include "docker_cleanup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "docker_cleanup.selectorLabels" . | nindent 4 }}
