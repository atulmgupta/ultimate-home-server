apiVersion: v1
kind: Service
metadata:
  name: {{ include "hchecks.fullname" . }}
  labels:
    {{- include "hchecks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hchecks.selectorLabels" . | nindent 4 }}
