apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "get_iplayer.fullname" . }}-test-connection"
  labels:
    {{- include "get_iplayer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "get_iplayer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
