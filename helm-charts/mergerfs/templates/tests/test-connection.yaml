apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mergerfs.fullname" . }}-test-connection"
  labels:
    {{- include "mergerfs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mergerfs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
