apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dozzle.fullname" . }}-test-connection"
  labels:
    {{- include "dozzle.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dozzle.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
