apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginxproxymanager.fullname" . }}-test-connection"
  labels:
    {{- include "nginxproxymanager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginxproxymanager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
