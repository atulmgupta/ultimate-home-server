apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "it_tools.fullname" . }}-test-connection"
  labels:
    {{- include "it_tools.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "it_tools.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
