apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "plex_meta_manager.fullname" . }}-test-connection"
  labels:
    {{- include "plex_meta_manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "plex_meta_manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
