apiVersion: v1
kind: Service
metadata:
  name: {{ include "plex_meta_manager.fullname" . }}
  labels:
    {{- include "plex_meta_manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "plex_meta_manager.selectorLabels" . | nindent 4 }}
