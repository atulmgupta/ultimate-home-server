apiVersion: v1
kind: Service
metadata:
  name: {{ include "rsnapshot.fullname" . }}
  labels:
    {{- include "rsnapshot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rsnapshot.selectorLabels" . | nindent 4 }}
