apiVersion: v1
kind: Service
metadata:
  name: {{ include "rssbridge.fullname" . }}
  labels:
    {{- include "rssbridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rssbridge.selectorLabels" . | nindent 4 }}
