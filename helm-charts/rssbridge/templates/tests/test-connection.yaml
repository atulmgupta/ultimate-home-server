apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rssbridge.fullname" . }}-test-connection"
  labels:
    {{- include "rssbridge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rssbridge.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
